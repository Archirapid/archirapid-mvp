# Sesión de trabajo — 21 Marzo 2026

## Estado al cerrar el día

### ✅ RESUELTO HOY

#### 1. Admin login — solo contraseña (sin email)
- **Archivo:** `modules/marketplace/auth.py`
- `show_login()` rama admin: eliminado campo email, compara directamente contra `ADMIN_PASSWORD` secret
- El botón "🔐 Admin" en `components/landing.py` y en el rol-selector de `app.py` navega directo a `Intranet` (bypasea el formulario inline, que estaba roto)
- La intranet tiene su propio gate con `ADMIN_PASSWORD` → funciona OK

#### 2. Registro → redirección correcta por rol
- **Archivo:** `modules/marketplace/auth.py`
- Antes: hardcodeaba `role='owner'` después de registrar (¡todos iban a Panel de Propietarios!)
- Ahora: redirige según rol real: client→Panel de Cliente, architect→Marketplace, owner→Propietarios

#### 3. Panel de Cliente mostraba Panel de Propietarios
- **Archivo:** `app.py` línea ~398
- `panel_cliente_v2()` llamaba a `owners.main()` en lugar de `client_panel.main()`
- Corregido con un cambio de una línea

#### 4. `id=NULL` en registro (Supabase)
- **Archivos:** `modules/marketplace/auth.py` y `app.py` (inline registration)
- Los INSERT no incluían la columna `id` → Supabase rechazaba por NOT NULL constraint
- Corregido generando `uuid.uuid4()` antes del INSERT en ambos sitios

#### 5. App lenta — SSL connection por cada query
- **Archivo:** `src/db.py`
- Añadido `ThreadedConnectionPool` (psycopg2) con singleton `@st.cache_resource`
- `ensure_tables()` wrapeado en `@st.cache_resource` → ya no corre 40+ DDL en cada rerun

#### 6. Intranet — `DuplicateWidgetID: key='approve_plot_id'`
- **Archivo:** `src/db.py` línea 78 — **BUG RAÍZ**
- `_CompatRow.__iter__` hacía `iter(self._data)` → iterar un dict da **claves**, no valores
- `dict(zip(cols, row))` en Postgres producía `{"id": "id", "title": "title", ...}`
- `p['id']` era el string `"id"` para todas las fincas → todas las keys Streamlit colisionaban
- **Fix:** `iter(self._data.values())` — 1 carácter, bug devastador
- **Commit:** `8eb7f9b`

---

### 🟡 PENDIENTE — PROPUESTA LISTA, NO EJECUTADA

#### MLS popup map buttons → páginas públicas (no login requerido)

**Problema actual:**
Los botones del popup naranja (Ver ficha / Reservar €200 / Solicitar info) redirigen todos a `"🏢 Inmobiliarias MLS"` que requiere login de inmobiliaria → el usuario ve el formulario de registro MLS.

**Solución diseñada:**
Crear `modules/mls/mls_publico.py` con 4 funciones públicas, y cambiar routing en `app.py`.

##### Archivo nuevo: `modules/mls/mls_publico.py`
Contiene:
- `show_ficha_publica(finca_id)` — ficha pública con mapa, fotos, specs, CTAs, proyectos compatibles
- `show_reservar_publico(finca_id)` — reutiliza `ui_formulario_reserva_cliente_directo(finca)` de `mls_reservas.py`
- `show_contacto_publico(finca_id)` — form nombre/email/tel/mensaje, reutiliza `mls_db.registrar_contacto_cliente()`
- `show_retorno_reserva_cliente(params)` — reutiliza `ui_handler_retorno_stripe()` de `mls_reservas.py`

##### `app.py` — Hunk 1 (líneas 1335-1359): cambiar routing
```
# ANTES (todos van al portal MLS con login):
if _qp_mls_ficha:
    st.session_state["selected_page"] = "🏢 Inmobiliarias MLS"
    st.session_state["mls_ficha_id"] = _qp_mls_ficha
# (mismo para mls_reservar y mls_contacto)

# DESPUÉS (páginas públicas sin login):
if _qp_mls_reserva == "1":
    if st.query_params.get("tipo") == "cliente_directo":
        st.session_state["selected_page"] = "_mls_retorno_cliente"
        st.session_state["mls_reserva_ok_params"] = {...}
    else:  # colaboradora → sigue al portal
        st.session_state["selected_page"] = "🏢 Inmobiliarias MLS"
        st.session_state["mls_reserva_ok_params"] = {...}

if _qp_mls_ficha:
    st.session_state["selected_page"] = "_mls_ficha_publica"
    st.session_state["mls_ficha_id"] = _qp_mls_ficha

if _qp_mls_reservar:
    st.session_state["selected_page"] = "_mls_reservar_publica"
    st.session_state["mls_reservar_id"] = _qp_mls_reservar

if _qp_mls_contacto:
    st.session_state["selected_page"] = "_mls_contacto_publica"
    st.session_state["mls_contacto_id"] = _qp_mls_contacto
```

##### `app.py` — Hunk 2 (tras línea 2428): añadir elif branches
```python
elif st.session_state.get('selected_page') == "_mls_ficha_publica":
    from modules.mls.mls_publico import show_ficha_publica
    show_ficha_publica(st.session_state.get("mls_ficha_id", ""))

elif st.session_state.get('selected_page') == "_mls_reservar_publica":
    from modules.mls.mls_publico import show_reservar_publico
    show_reservar_publico(st.session_state.get("mls_reservar_id", ""))

elif st.session_state.get('selected_page') == "_mls_contacto_publica":
    from modules.mls.mls_publico import show_contacto_publico
    show_contacto_publico(st.session_state.get("mls_contacto_id", ""))

elif st.session_state.get('selected_page') == "_mls_retorno_cliente":
    from modules.mls.mls_publico import show_retorno_reserva_cliente
    show_retorno_reserva_cliente(st.session_state.get("mls_reserva_ok_params", {}))
```

**Principios de diseño:**
- Zero duplicación — reutiliza lógica Stripe, DB y formularios ya existentes
- Seguridad — `get_finca_sin_identidad_listante()` garantiza que `inmo_id` nunca se expone
- No rompe flujo colaboradora — `tipo=colaboradora` sigue al portal con login
- No rompe `mls_pago=ok` — ese flujo no toca

---

### 🔴 BUGS CONOCIDOS PENDIENTES (no urgentes)

- `ImportError: cannot import name 'flow' from 'modules.ai_house_designer'` — aparece en logs durante redeploy, se recupera solo. Posible issue Python 3.14 + `__init__.py`
- `KeyError: 'src.db'` en logs — transiente durante hot-reload, no persiste

---

## Commits del día

| Hash | Mensaje |
|------|---------|
| `8eb7f9b` | fix: _CompatRow.__iter__ yields values not keys — fixes duplicate widget keys in Postgres |
| `bd67cb7` | (último commit antes de esta sesión) |

---

## Para continuar mañana

**Orden para Claude:** "Lee SESION_2026-03-21.md y ejecuta la propuesta MLS pendiente"

El documento tiene todo el diff completo. Solo ejecutar:
1. Crear `modules/mls/mls_publico.py` con el código de la propuesta
2. Modificar `app.py` Hunk 1 (líneas 1335-1359)
3. Modificar `app.py` Hunk 2 (tras línea 2428)
4. Commit y push

---

## Archivos clave del proyecto

| Archivo | Propósito |
|---------|-----------|
| `app.py` | Router principal Streamlit (~2500 líneas) |
| `src/db.py` | Capa de datos SQLite/Postgres, pool de conexiones |
| `modules/marketplace/auth.py` | Login y registro de usuarios |
| `modules/marketplace/intranet.py` | Panel admin |
| `modules/mls/mls_mapa.py` | Pins naranjas en el mapa home |
| `modules/mls/mls_reservas.py` | Lógica Stripe para reservas MLS |
| `modules/mls/mls_db.py` | DB layer para tablas MLS |
| `modules/mls/mls_portal.py` | Portal para inmobiliarias (requiere login) |
| `components/landing.py` | Página home con mapa |

## Deploy
- GitHub: https://github.com/Archirapid/archirapid-mvp.git (branch: main)
- Streamlit Cloud: https://archirapid.streamlit.app
